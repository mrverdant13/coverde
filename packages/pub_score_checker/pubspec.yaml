name: pub_score_checker
description: Evaluate the health and quality of a Dart package.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  mocktail: ^1.0.0
  test: ^1.24.6
  very_good_analysis: ^5.0.0+1
dependencies:
  pana: any # Using the latest version of `pana` to ensure that the package score computation method is also up-to-date.
